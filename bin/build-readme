#!/bin/bash

set -euo pipefail

plugin_dir="$(cd "$(dirname "$0")/.." && pwd)"

n-sync-plug-to-zima-sym.nu tweakmaster
tweaks=$(wp @stage tweakmaster feature_list)
content=$(<"$plugin_dir/readme-src.md")

echo "${content/\{ tweaks \}/$tweaks}" > "$plugin_dir/readme.txt"
